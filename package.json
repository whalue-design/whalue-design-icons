{
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "bootstrap:china": "lerna bootstrap --registry=https://registry.npm.taobao.org",
    "icons:generate": "lerna run --scope @whalue-design/icons-svg generate",
    "icons:build": "lerna run --scope @whalue-design/icons-svg build",
    "icons:test": "lerna run --scope @whalue-design/icons-svg test",
    "lint": "lerna run lint"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "license": "MIT"
}
